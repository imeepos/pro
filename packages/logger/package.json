{
  "name": "@pro/logger",
  "version": "1.0.0",
  "description": "Framework-agnostic Pino logger for Pro monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "clean": "rimraf dist",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pino": "^9.13.1",
    "pino-http": "^10.5.0",
    "pino-pretty": "^13.1.2"
  },
  "devDependencies": {
    "@pro/eslint-config": "workspace:*",
    "@pro/prettier-config": "workspace:*",
    "tsup": "^8.5.0",
    "typescript": "~5.8.0"
  }
}
