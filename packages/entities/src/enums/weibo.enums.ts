export enum WeiboVisibleType {
  Public = 'public',
  Fans = 'fans',
  Group = 'group',
  Private = 'private',
  Custom = 'custom',
}

export enum WeiboInteractionType {
  Like = 'like',
  Repost = 'repost',
  Comment = 'comment',
  Favorite = 'favorite',
}

export enum WeiboTargetType {
  Post = 'post',
  Comment = 'comment',
}

export enum WeiboMediaType {
  Image = 'image',
  Video = 'video',
  Audio = 'audio',
  Article = 'article',
  Unknown = 'unknown',
}
